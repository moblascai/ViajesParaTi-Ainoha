<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inicio - Proveedor Form</title>
    <link rel="stylesheet" href="{{ asset('css/styles.css') }}"> <!-- Enlace al archivo CSS -->
</head>
<body>
    <div class="container">
        <h1 class="text-center mt-4">Inicio</h1>
        <div class="buttons-container">
            <a href="{{ path('app_proveedor_crear') }}" class="btn btn-create">Crear</a>
            <a href="{{ path('app_proveedor_listado') }}" class="btn btn-list">Listado</a>
        </div>
    </div>
</body>
</html>